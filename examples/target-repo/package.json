{
  "name": "target-repo",
  "version": "0.0.1",
  "description": "Toy TypeScript project used as the worker sandbox for hero-coding demos. Intentionally contains a few bugs.",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --import tsx --test tests/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
