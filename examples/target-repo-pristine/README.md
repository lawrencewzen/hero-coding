# target-repo

Tiny Go module used as the hero-coding worker sandbox. Intentionally ships with one off-by-one bug in `utils.ParseRange` so a demo story has something to fix.

```bash
go test ./...   # initially fails
```
