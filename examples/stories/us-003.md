---
id: us-003
title: formatNumber prints garbage on negative numbers
created: 2026-04-28
priority: high
max_retries: 2
---

## Goal

A user reported that `formatNumber` "looks broken on negative numbers". Reproduce the issue, find the cause in `src/utils.ts`, fix it, and prove it stays fixed.

## Acceptance Criteria

- [ ] `npm test` is fully green.
- [ ] Add at least 2 new tests in `tests/utils.test.ts` that would have caught the bug — at least one for an integer (e.g. `-1234`) and one for a fractional value (e.g. `-1234.5`).
- [ ] Behavior on positive numbers and zero is unchanged.

## Constraints

- Touch only `src/utils.ts` and `tests/utils.test.ts`.
- Keep TypeScript strict mode happy.

## Out of Scope

- Locale / currency formatting.
- Custom precision options.
