---
id: us-001
title: Fix off-by-one in ParseRange
priority: normal
max_retries: 3
verify:
  - go test ./...
scope:
  - utils/**
---

## Goal

`utils.ParseRange("1-5")` must return `[1, 2, 3, 4, 5]`, but currently returns `[1, 2, 3, 4]`. The existing test in `utils/utils_test.go` already covers this and is failing. Make it pass.

## Acceptance Criteria

- [ ] `ParseRange("1-5")` returns `[1, 2, 3, 4, 5]`.
- [ ] `ParseRange("3-3")` returns `[3]`.
- [ ] `go test ./...` is fully green.

## Constraints

- Touch only `utils/utils.go` (and tests if you want to add more).
- Do not change the function signature.

## Out of Scope

- Validating that `a <= b`.
- Supporting comma-separated lists or step values.
