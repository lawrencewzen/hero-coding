---
id: us-002
title: Fix off-by-one in parseRange
created: 2026-04-28
priority: high
max_retries: 3
---

## Goal

`parseRange("1-5")` must return `[1, 2, 3, 4, 5]`, but currently returns `[1, 2, 3, 4]`. There's an existing failing test in `tests/utils.test.ts` covering this. Make it pass.

## Acceptance Criteria

- [ ] `parseRange("1-5")` returns `[1, 2, 3, 4, 5]`.
- [ ] `parseRange("3-3")` returns `[3]`.
- [ ] All existing tests continue to pass.
- [ ] `npm test` is fully green.

## Constraints

- Touch only `src/utils.ts` and possibly add tests in `tests/utils.test.ts`.
- Do not change the function signature.

## Out of Scope

- Validating that `a <= b`.
- Supporting comma-separated lists or step values.
