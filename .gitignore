node_modules/
dist/
*.log

# Go build artifacts
/hero
/bin/
*.test
*.out
coverage.out

# secrets
.env
.env.local
config.local.yaml
*.local.yaml

# runtime working dirs
workspace/
inbox/_processing/
done/_archive/

# stats / runs
runs/

# target-repo carries its own .git when seeded; don't commit it as nested repo
examples/target-repo/.git/

# inbox / done are runtime, the example stories live under examples/stories/
inbox/*.md
done/*.md

# editor
.DS_Store
.vscode/
.idea/
.obsidian/
.claude/scheduled_tasks.lock
examples/target-repo/package-lock.json
examples/target-repo/node_modules/
